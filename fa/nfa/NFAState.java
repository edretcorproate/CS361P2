package fa.nfa;

import fa.State;

public class NFAState extends State {
	
    public NFAState (String name) {
        /*
        Call the superclass constructor (as per the documentation) with the given state name
         */
        super(name);
    }

}
